name: Validate Metadata

on:
  pull_request:
    branches: [main]
    paths:
        - '**/modernization-metadata/**'

permissions:
  pull-requests: write

jobs:
  call-validate-metadata:
    uses: CodexRaunak/plugin-modernizer-tool/.github/workflows/validate-metadata.yml@cleanup-metadata